git config --global user.name "Itamar Lachman"
git config --global user.email "itamarlachman@talenya.com"
git clone https://itamarlachman@github.com/itamarlachman/repository.git

git config  user.name "Itamar Lachman"
git config  user.email "itamarlachman@talenya.com"